# Install

## devcontainer.common.json
```bash
bash <(curl https://suede.sh/utils/degit) --destination . --repo pmalacho/mit --branch common --force
```

## devcontainer.docker-outside-of-docker.json
```bash
bash <(curl https://suede.sh/utils/degit) --destination . --repo pmalacho/mit --branch docker-outside-of-docker --force
```

## devcontainer.node-default.json
```bash
bash <(curl https://suede.sh/utils/degit) --destination . --repo pmalacho/mit --branch node-default --force
```

## devcontainer.poetry-default.json
```bash
bash <(curl https://suede.sh/utils/degit) --destination . --repo pmalacho/mit --branch poetry-default --force
```

## devcontainer.python-default.json
```bash
bash <(curl https://suede.sh/utils/degit) --destination . --repo pmalacho/mit --branch python-default --force
```

## devcontainer.svelte-default.json
```bash
bash <(curl https://suede.sh/utils/degit) --destination . --repo pmalacho/mit --branch svelte-default --force
```

## devcontainer.svelte-tailwind-default.json
```bash
bash <(curl https://suede.sh/utils/degit) --destination . --repo pmalacho/mit --branch svelte-tailwind-default --force
```

## devcontainer.typescript-default.json
```bash
bash <(curl https://suede.sh/utils/degit) --destination . --repo pmalacho/mit --branch typescript-default --force
```

